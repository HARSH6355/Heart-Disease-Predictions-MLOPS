# EDA Notes

Use this file to summarize findings from `notebooks/EDA.ipynb` for the final report.

Suggested points to include after rerunning the notebook:

- Dataset shape and feature list.
- Missing values in `ca` and `thal`, and how preprocessing handles them.
- Target class balance after converting `num` to binary `target`.
- Correlation patterns with `target`.
- Visual observations from chest pain type, maximum heart rate, oldpeak, and thalassemia-related features.

The assignment asks for professional visualizations. Keep screenshots of the class balance chart, correlation heatmap, and at least two feature-vs-target plots in `docs/screenshots/`.
