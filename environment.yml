name: heart-disease-mlops
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - matplotlib
      - seaborn
      - flask
      - dill
      - mlflow
      - xgboost
      - catboost
      - ucimlrepo
      - pytest
      - evidently
      - prometheus-client
      - gunicorn
